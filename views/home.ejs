<%- include('partials/header') %>

<main>
  <div class="hero">
    <h2>Welcome to Chatbot Builder</h2>
    <p>Create, customize, and deploy your own AI chatbots in minutes</p>
    
    <% if (!isAuthenticated) { %>
      <div class="cta-buttons">
        <a href="/auth/register" class="btn">Get Started</a>
        <a href="/auth/login" class="btn">Login</a>
      </div>
    <% } else { %>
      <div class="cta-buttons">
        <a href="/create-bot" class="btn">Create New Bot</a>
        <a href="/explore" class="btn">Explore Bots</a>
      </div>
    <% } %>
  </div>

  <section class="features">
    <h3>Features</h3>
    <div class="feature-grid">
      <div class="feature-card">
        <h4>🤖 Custom Bots</h4>
        <p>Create personalized chatbots tailored to your needs</p>
      </div>
      <div class="feature-card">
        <h4>🔌 Easy Embedding</h4>
        <p>Integrate your chatbot anywhere with a simple code snippet</p>
      </div>
      <div class="feature-card">
        <h4>🎨 Customizable</h4>
        <p>Customize your bot's personality, tone, and abilities</p>
      </div>
      <div class="feature-card">
        <h4>📱 Responsive</h4>
        <p>Works perfectly on all devices and screen sizes</p>
      </div>
    </div>
  </section>
</main>

<style>
.hero {
  text-align: center;
  padding: 4rem 1rem;
}

.hero h2 {
  font-size: 2.5rem;
  margin-bottom: 1rem;
  background: linear-gradient(to right, #60a5fa, #3b82f6);
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
}

.hero p {
  font-size: 1.2rem;
  color: var(--text-secondary);
  margin-bottom: 2rem;
}

.cta-buttons {
  display: flex;
  gap: 1rem;
  justify-content: center;
}

.features {
  padding: 2rem 1rem;
}

.features h3 {
  text-align: center;
  font-size: 2rem;
  margin-bottom: 2rem;
}

.feature-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2rem;
  padding: 1rem;
}

.feature-card {
  background: var(--card-bg);
  padding: 1.5rem;
  border-radius: 1rem;
  text-align: center;
}

.feature-card h4 {
  font-size: 1.2rem;
  margin-bottom: 1rem;
}

.feature-card p {
  color: var(--text-secondary);
}

@media (max-width: 768px) {
  .hero h2 {
    font-size: 2rem;
  }
  
  .cta-buttons {
    flex-direction: column;
  }
  
  .feature-grid {
    grid-template-columns: 1fr;
  }
}
</style>

<%- include('partials/footer') %>